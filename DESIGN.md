# DESIGN.md — Kindle Library 🎀

「ピンク基調 × パステル × サブカルチック」をテーマにした、自分の Kindle 蔵書を眺めて楽しむためのギャラリーサイトのデザインガイド。
実装(Tailwind v4 のテーマトークン / コンポーネント)はこのドキュメントを正とする。

---

## 1. コンセプト

> **「乙女な本棚をのぞき見る」** — ゆめかわ・量産系のポップさに、同人誌即売会のステッカーやzineの手作り感を一滴。

- かわいいけど子どもっぽくしすぎない。彩度はパステルで抑え、要所にビビッドピンクを「差す」。
- 表紙(書影)が主役。UI はあくまで額縁・台紙として控えめに、しかし遊び心を残す。
- 情報量(1,200冊超)が多いので、**かわいさと一覧性の両立**を最優先する。

---

## 2. カラーパレット

### ベース(背景・台紙)
| 役割 | トークン | HEX | 用途 |
|---|---|---|---|
| 背景・ベース | `cream` | `#fff5f9` | ページ全体の地。ほんのり桜色 |
| 背景・グラデ終点 | `lavender-mist` | `#f3eefb` | 上から下へ桜色→藤色のグラデ |
| カード地 | `surface` | `#ffffff` | 書影カードの台紙 |
| 枠線・区切り | `border-soft` | `#ffe0ec` | 1px のやさしいピンク罫 |

### アクセント(ポップに差す色)
| 役割 | トークン | HEX | 用途 |
|---|---|---|---|
| メイン | `pink` | `#ff7eb6` | ロゴ・主要ボタン・アクティブ状態 |
| 強調 | `pink-vivid` | `#ff4f9e` | ホバー・選択・バッジの主張 |
| サブ1 | `lavender` | `#c8b6ff` | フィルタチップ・サブ装飾 |
| サブ2 | `mint` | `#9ee6c4` | READ バッジ・成功系 |
| サブ3 | `lemon` | `#ffe9a8` | ハイライト・キラ装飾 |

### テキスト
| 役割 | トークン | HEX | メモ |
|---|---|---|---|
| 本文 | `ink` | `#5b3a52` | 黒ではなく濃いプラム。ピンク地で目に優しい |
| 副次 | `ink-soft` | `#9b7d92` | キャプション・著者名 |
| 反転 | `on-pink` | `#ffffff` | ピンク背景上の文字 |

> **使い分けの原則**: 画面の8割はベース＋白＋inkで「静」。ピンク/ラベンダー/ミントは合計2割以内で「動」。色を盛りすぎない。

---

## 3. タイポグラフィ

Google Fonts を使用(`root.tsx` の `links` で読み込み)。

| 用途 | フォント | 指定 |
|---|---|---|
| ロゴ・大見出し | **Mochiy Pop One** | ぽってり丸い、サブカル/量産系の象徴。1ワード〜短文のみ |
| 本文・UI | **M PLUS Rounded 1c** | 丸ゴシック。weight 400/500/700 を使用 |
| 数字・英字の装飾 | M PLUS Rounded 1c (700) | 統計の数字などはやや大きく |

- 基本サイズ: 14px(`text-sm`)を本文基準に。タイトルは詰まりがちなので行間 `leading-snug`。
- 日本語タイトルは2行で省略(`line-clamp-2`)。著者名は1行省略。
- 見出しに句読点的な絵文字(🎀 ✨ 📚 🩷)を1つだけ添えてよい。乱用しない。

---

## 4. モチーフ & テクスチャ

サブカル感は「装飾の手数」で出す。ただし**書影の邪魔をしない**範囲で。

- **ステッカー風カード**: 角丸大きめ(`rounded-2xl`)＋白フチ＋ピンクの淡いドロップシャドウ。
- **ドット背景**: ページ地に薄い水玉(`radial-gradient` のCSS、`border-soft` 色で 2% 程度の存在感)。
- **キラ ✨**: アクティブ要素やREADバッジにスパークルを1つ。CSSの擬似要素で。
- **波線・スカラップ**: ヘッダー下端を波形(SVG or `mask`)にして台紙感を出す。
- **テープ風**: セクション見出しの脇に、半透明のマスキングテープ風の矩形を傾けて配置(任意)。

> アニメや版権モチーフは使わない。形・色・余白で「サブカルっぽさ」を作る。

---

## 5. コンポーネント仕様

### 5.1 ヘッダー
- 左にロゴ「🎀 きんどる本棚」(Mochiy Pop One)。
- 右or下に**統計バッジ**: 「全 1,204冊 / 既読 833 / 未確認 371 / シリーズ 438」をパステルのピル(pill)で。
- 下端は波線(スカラップ)。背景は cream→lavender-mist の縦グラデ。
- スクロール追従(`sticky top-0`)。スクロール時は半透明＋blur(`backdrop-blur`)。

### 5.2 ツールバー(検索・絞り込み)
- **検索バー**: 角丸ピル、左に🔍、プレースホルダ「タイトル・著者で探す」。フォーカスで pink リング。
- **ステータスフィルタ**: チップ「すべて / 既読 / 未確認」。選択中は pink 塗り＋✨。
- **ソート**: ドロップダウン「追加日が新しい順 / タイトル / 著者」。
- **シリーズまとめトグル**: スイッチ「シリーズでまとめる」。ON で同一シリーズを1枚に集約し、巻数バッジ表示。
- モバイルでは折りたたみ/横スクロール。常に sticky 圏内に置く。

### 5.3 書影カード(主役)
- 縦長(書影アスペクト ≒ 2:3)。`rounded-2xl`、白フチ、ホバーで `-translate-y-1` ＋影濃く。
- 画像下に **タイトル(2行省略)** と **著者(1行・ink-soft)**。
- 左上に**ステータスバッジ**(READ=mint+✨ / UNKNOWN=lavender)。
- 巻数があれば右上に**巻数バッジ**(`#3` など、pink ピル)。
- クリックで Amazon 商品ページを新規タブ(`target=_blank` `rel=noreferrer`)。
- **書影フォールバック**: 画像取得失敗(`onError`)時は、シリーズ色から決まるパステルのグラデ地＋タイトル頭文字＋📖 のプレースホルダーを表示。崩れさせない。

### 5.4 シリーズまとめカード(トグルON時)
- 代表(最小巻 or 1巻)の書影を大きく、背後に重なった紙(2〜3枚)を覗かせて「束」を表現。
- 「全N巻」バッジ。クリックで展開 or そのまま代表巻へ。

### 5.5 バッジ / ピル
- 形は完全な角丸(`rounded-full`)、小さめ、`text-xs`、`font-medium`。
- READ: `mint` 地 / 濃ミント文字。UNKNOWN: `lavender` 地 / 濃ラベンダー文字。

### 5.6 空状態 / 0件
- 検索ヒット0件は「みつからなかった…🍰」とゆるいイラスト的余白で。気落ちさせない文言。

---

## 6. レイアウト & レスポンシブ

- 最大幅 `max-w-7xl` 中央寄せ、左右 `px-4〜8`。
- **グリッド列数**(書影):
  - モバイル(〜640px): **2列**
  - sm(640+): 3列
  - md(768+): 4列
  - lg(1024+): 5列
  - xl(1280+): **6列**
- グリッド間隔 `gap-4`(モバイル)〜`gap-6`(PC)。
- ヘッダー・ツールバーは sticky。本文はスクロール領域。

---

## 7. モーション

- 控えめ＆ふわっと。`transition` は 150〜250ms、`ease-out`。
- カード hover: `translate-y` ＋ shadow。タップ(モバイル)では `active:scale-95`。
- フィルタ/検索の結果切替: 軽いフェード(`opacity`)。並べ替えのレイアウトアニメは無理にやらない(件数が多く重くなるため)。
- 初回表示: 1stビューのカードのみ軽いフェードイン(段差は最小限、`prefers-reduced-motion` を尊重)。
- ✨ のきらめきは2〜3秒の緩いループ。`prefers-reduced-motion: reduce` では停止。

---

## 8. アクセシビリティ

- ピンク地×白文字は十分なコントラストを確保(濃いめ pink-vivid を文字背景に使う)。本文は ink を白/cream 上に。
- 画像 `alt` はタイトル。装飾のみの絵文字は `aria-hidden`。
- フォーカスリングを必ず可視化(pink の `ring-2`)。
- リンクカードはキーボード操作可(`<a>` ベース)。
- `prefers-reduced-motion` を尊重しアニメを抑制。

---

## 9. Tailwind トークン対応(`app/app.css` の `@theme`)

```css
@theme {
  --color-cream: #fff5f9;
  --color-lavender-mist: #f3eefb;
  --color-surface: #ffffff;
  --color-border-soft: #ffe0ec;
  --color-pink: #ff7eb6;
  --color-pink-vivid: #ff4f9e;
  --color-lavender: #c8b6ff;
  --color-mint: #9ee6c4;
  --color-lemon: #ffe9a8;
  --color-ink: #5b3a52;
  --color-ink-soft: #9b7d92;
  --font-display: "Mochiy Pop One", sans-serif;
  --font-sans: "M PLUS Rounded 1c", ui-sans-serif, system-ui, sans-serif;
}
```

> 実装時はこのトークンを基準に `bg-pink` / `text-ink` / `font-display` などで参照する。
> 新しい色・フォントを足したくなったら、まずこの表とトークンを更新してから使うこと。
